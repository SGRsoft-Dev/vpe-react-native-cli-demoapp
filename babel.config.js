module.exports = {
  presets: ['module:@react-native/babel-preset'],
  plugins: [
    // react-native-reanimated/plugin is NOT required by this demo.
    // react-native-gesture-handler needs no babel plugin.
  ],
};
